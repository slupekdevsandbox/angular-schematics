name: CI

on: [push]

jobs:
  build_and_test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v1
    - name: Run Yarn
      run: yarn
    - name: Bootstrap & Build packages
      run: yarn ci:build   
    #- name: Run tests
    #  run: yarn ci:test
    - name: Setup & run packages on sandbox
      run: yarn ci:sandbox
      #run: yarn sandbox:install && yarn sandbox:link && yarn sandbox:run:all
    #- name: Setup & run packages on sandbox
    #  run: git diff


